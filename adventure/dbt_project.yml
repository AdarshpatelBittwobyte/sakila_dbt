name: 'adventure'
version: '1.0.0'
config-version: 2

profile: 'adventure'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:

  dbt_projects:
      +materialized: table

on-run-end:
  - "{{update_etl_time() }}"